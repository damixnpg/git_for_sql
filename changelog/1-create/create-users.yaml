databaseChangeLog:
  - changeSet:
      id: 1
      author: junior
      changes:
        - sql: |
            CREATE TABLE IF NOT EXISTS users (
              id SERIAL PRIMARY KEY,
              name VARCHAR(100),
              email VARCHAR(100)
            );
 